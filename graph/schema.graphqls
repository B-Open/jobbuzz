# GraphQL schema example
#
# https://gqlgen.com/getting-started/

input NewJob {
  text: String!
  userId: String!
}

type Job {
  id: Int!
  title: String!
}

type Query {
  jobs: [Job!]!
}

type Mutation {
  createJob(input: NewJob!): Job!
}
